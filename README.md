# Sandwich Project Collection Sites

## Where the app lives

Firebase Hosting serves the `public/` folder. Edit `public/app.js`, then run `npm run build:js` so `public/app.build.js` stays current. The root `app.js` is an old copy and is not what the live site loads.
